#include "smblib.h"

#define HEAPSIZE 1024
/*
typedef struct smb_diritem_t 
{
	char host[MAX_HOST_LEN];
	char share[MAX_SHARE_LEN];
	char dir[MAX_DIR_LEN];
	void *next;
	int index; // for special memory allocation shiznits
	int next_idx;
}

typedef struct smb_mem_item_t 
{
	smb_diritem_t item;
	int used;
}smb_mem_item_t;

int allocate_heap(void *heap, int size);
int deallocate_heap(void *heap, int size);

int find_freeslot(void *heap, int size);
int free_slot(void *heap, int slot);
int alloc_slot(void *heap, int size, int slot);
*/
	
